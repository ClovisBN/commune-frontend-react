// DateQuestion.jsx
import React from "react";

const DateQuestion = ({ question, onChange, isDisabled = true }) => {
  return (
    <div className="date-question">
      <div className="prview-survey-question">Reponse date</div>
    </div>
  );
};

export default DateQuestion;

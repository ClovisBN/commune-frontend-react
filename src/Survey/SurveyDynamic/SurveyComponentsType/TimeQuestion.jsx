import React from "react";

const TimeQuestion = ({ question, onChange, isDisabled = true }) => {
  return (
    <div className="time-question">
      <div className="prview-survey-question">Reponse Horaire</div>
    </div>
  );
};

export default TimeQuestion;
